---
const { title, description } = Astro.props;
---
<section class="bg-theme-bg text-theme-text py-12 text-center border-b border-theme-secondary">
  <h2 class="text-4xl font-bold mb-4">{title}</h2>
  <p class="text-lg mb-6">{description}</p>
  <button class="px-4 py-2 rounded font-semibold" style="background-color: var(--color-accent); color: var(--color-bg);">
    Explore Posts
  </button>
</section>